# Trivy Ignore File for Audio Processor
# This file documents vulnerabilities that are accepted risks or cannot be fixed yet.
# Each entry should have a clear justification.

# CVE-2025-13601 - libglib2.0-0t64 heap buffer overflow in g_escape_uri_string()
# Status: No fix available in Debian as of 2025-12-06
# Severity: HIGH
# Risk Assessment: LOW for this application
# Justification:
#   - Vulnerability requires calling g_escape_uri_string() with a very large number
#     of unacceptable characters that need escaping
#   - This application does not use GLib URI escaping functionality
#   - Upstream patches exist (glib 2.86.2) but not yet backported to Debian
#   - Tracking: Debian bug #1121488
# References:
#   - https://security-tracker.debian.org/tracker/CVE-2025-13601
#   - https://gitlab.gnome.org/GNOME/glib/-/issues/3827
#   - https://gitlab.gnome.org/GNOME/glib/-/merge_requests/4914
# Review Date: 2025-12-06
# Next Review: When Debian releases patch (monitor bug #1121488)
CVE-2025-13601

# CVE-2025-64720, CVE-2025-65018, CVE-2025-66293 - libpng16-16t64 vulnerabilities
# Status: No fix available in Debian 13.2 as of 2025-12-06
# Severity: HIGH (all three)
# Risk Assessment: LOW for this application
# Justification:
#   - CVE-2025-64720: Buffer overflow in png_image_read_composite
#   - CVE-2025-65018: Heap buffer overflow in libpng
#   - CVE-2025-66293: Out-of-bounds read in png_image_read_composite
#   - Application uses libpng only through Python libraries (Pillow, matplotlib)
#   - No direct manipulation of PNG internals or untrusted PNG files in processing pipeline
#   - Debian security tracker shows "affected" status with no fix version yet
# References:
#   - https://security-tracker.debian.org/tracker/CVE-2025-64720
#   - https://security-tracker.debian.org/tracker/CVE-2025-65018
#   - https://security-tracker.debian.org/tracker/CVE-2025-66293
# Review Date: 2025-12-06
# Next Review: When Debian releases patched version
CVE-2025-64720
CVE-2025-65018
CVE-2025-66293

# CVE-2026-40393 - libgbm1 / mesa-libgallium (Mesa GBM / Gallium)
# Status: will_not_fix (Debian security tracker)
# Severity: CRITICAL
# Risk Assessment: NEGLIGIBLE for this application
# Justification:
#   - Vulnerability is in Mesa's hardware-accelerated graphics buffer
#     management (GBM) and Gallium driver layer
#   - This is a server-side audio processing service: no display server,
#     no GPU access, no graphics rendering, headless container
#   - libgbm1 / mesa-libgallium are pulled in only as transitive deps of
#     libavcodec-extra (ffmpeg) for hardware video acceleration codepaths
#     that this application never exercises
#   - Container runs as non-root user with no /dev/dri access
# References:
#   - https://security-tracker.debian.org/tracker/CVE-2026-40393
# Review Date: 2026-05-18
# Next Review: 2026-07-17 (60-day cap per CLAUDE.md unfixed-CVE policy)
CVE-2026-40393

# CVE-2026-5673 - libtheoradec1 (Theora video decoder)
# Status: fix_deferred (Debian security tracker)
# Severity: HIGH
# Risk Assessment: NEGLIGIBLE for this application
# Justification:
#   - libtheoradec1 decodes Ogg Theora video streams
#   - This is an audio processing service; it does not decode video
#   - libtheoradec1 is pulled in transitively by libavcodec-extra (ffmpeg)
#     for completeness of codec support, but the application never invokes
#     Theora decoding via ffmpeg
#   - Debian has classified this as fix_deferred, meaning low-impact in
#     typical Debian deployments
# References:
#   - https://security-tracker.debian.org/tracker/CVE-2026-5673
# Review Date: 2026-05-18
# Next Review: 2026-07-17 (60-day cap per CLAUDE.md unfixed-CVE policy)
CVE-2026-5673

# ----------------------------------------------------------------------
# Base-image transitive C library CVEs (Debian 13 trixie, python:3.12-slim)
# ----------------------------------------------------------------------
# The remaining suppressions below cover CVEs in C libraries that ship in
# the python:3.12-slim base image as transitive dependencies of build tools
# (curl, git), ffmpeg's libavcodec-extra, and standard glibc-adjacent system
# libraries (ncurses, expat, libxml2). None of these libraries are exercised
# by the application's request path:
#
#   - All outbound TLS in Python code goes through OpenSSL via the
#     'cryptography' and 'urllib3' packages, NOT GnuTLS, mbedTLS, libssh-4,
#     or libssh2-1t64. The C-level TLS libraries are present only because
#     curl and ffmpeg link them.
#   - 'curl' is used only for image-build operations (downloading UV in the
#     builder stage) and runtime healthchecks against localhost. It does
#     not handle untrusted remote endpoints in the request path.
#   - 'libexpat1' and 'libxml2' are not invoked anywhere in src/ (verified
#     by grep). No Python code uses xml.etree, lxml, or any XML parser.
#   - 'ncurses' (libncursesw6, libtinfo6, ncurses-base) is a terminal
#     capability library; this is a headless server with no TTY rendering.
#   - 'libsndfile1' is a declared Python dependency for future audio I/O,
#     but no application code currently invokes it (verified by grep).
#     CVE-2026-37555 is in the IMA ADPCM reader; even when audio I/O is
#     wired up, the application will not process untrusted IMA-encoded WAV
#     inputs.
#
# The container also runs as a non-root user (appuser, UID 1000) with no
# privileged operations or device access. Each block below names the CVE
# family, severity inventory, and the package family. All entries have
# Review Date: 2026-05-18 and Next Review: 2026-07-17 (60-day reassess cap
# per CLAUDE.md unfixed-CVE policy). All details documented in
# docs/known-vulnerabilities.md under "Base-image transitive library CVEs".

# curl / libcurl4t64 - HTTP transfer library
#   CVE-2026-5773 (HIGH, affected): wrong SMB file transfer
#   CVE-2026-6276 (HIGH): cookie information disclosure
CVE-2026-5773
CVE-2026-6276

# libexpat1 - XML parser library
#   CVE-2026-25210 (HIGH): information disclosure / data corruption
#   CVE-2026-45186 (HIGH): computational complexity DoS
CVE-2026-25210
CVE-2026-45186

# libgnutls30t64 - GnuTLS transport security
#   CVE-2026-33845 (CRITICAL): DTLS zero-length DoS
#   CVE-2026-33846 (HIGH, affected): DoS via heap buffer overflow
#   CVE-2026-42009 (HIGH, affected): DoS via DTLS packet reordering
#   CVE-2026-42010 (HIGH): authentication bypass via NUL character
#   CVE-2026-3833  (HIGH): policy bypass due to case-sensitive comparison
#   CVE-2026-42011 (HIGH): security bypass via incorrect name validation
CVE-2026-33845
CVE-2026-33846
CVE-2026-42009
CVE-2026-42010
CVE-2026-3833
CVE-2026-42011

# libmbedcrypto16 - Mbed TLS cryptography
#   CVE-2026-34873 (CRITICAL): client impersonation during TLS 1.3
#   CVE-2026-34875 (HIGH): arbitrary code execution
#   CVE-2026-25835 (HIGH): improper API misuse
#   CVE-2026-34872 (HIGH): shared secret leak
CVE-2026-34873
CVE-2026-34875
CVE-2026-25835
CVE-2026-34872

# ncurses family (libncursesw6, libtinfo6, ncurses-base)
#   CVE-2025-69720 (HIGH): buffer overflow may lead to code execution
CVE-2025-69720

# libsndfile1 - audio I/O (currently unused; dependency reserved for future use)
#   CVE-2026-37555 (HIGH, fix_deferred): integer overflow in ima_reader_init
CVE-2026-37555

# libssh-4 - SSH client library (transitive via curl/git)
#   CVE-2026-0966 (HIGH, affected): DoS via zero-length input
#   CVE-2026-3731 (HIGH): DoS via out-of-bounds read
CVE-2026-0966
CVE-2026-3731

# libssh2-1t64 - SSH2 client library (transitive via curl)
#   CVE-2026-7598 (CRITICAL): integer overflow via large username/password
CVE-2026-7598

# libxml2 - XML processing library
#   CVE-2026-6732 (HIGH): DoS via crafted XML input
CVE-2026-6732
